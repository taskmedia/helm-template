apiVersion: v1
kind: Service
metadata:
  name: {{ include "other-demo.fullname" . }}
  labels:
    {{- include "other-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "other-demo.selectorLabels" . | nindent 4 }}
