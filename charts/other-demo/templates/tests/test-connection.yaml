apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "other-demo.fullname" . }}-test-connection"
  labels:
    {{- include "other-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "other-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
